{
  "name": "teamflect-notifier",
  "version": "1.0.0",
  "private": true,
  "description": "Local frontend and narrowly scoped proxy for Teamflect feedback requests",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
